# 影策插件架构与接入说明



## 1\. 核心结论

影策 的插件体系是一个以 Provider 为中心的声明式协议适配平台。插件包描述模型或外部生成服务的请求构造、参数映射、响应解析、异步轮询、取消和结果获取；影策宿主负责真正的凭据读取、网络访问、SSRF 防护、任务生命周期、计费、媒体下载、资源持久化和错误处理。

它的主要价值是：

- 用统一的 GenerationRequest 屏蔽不同模型服务的请求差异；

- 用声明式 Manifest 映射不同厂商的 HTTP、multipart、云签名和异步任务协议；

- 用受限 JSON 表达式处理数组、条件、媒体 role、首尾帧和厂商参数；

- 用一个 Provider Registry 将协议能力投影给管理员渠道、用户渠道、画布、创作页和 Agent；

- 允许官方维护大量独立协议包，而不用为每个上游服务新增一套宿主业务分支。

```text
插件 ZIP
  -> manifest.json
  -> ParsePluginPackage
  -> ValidateManifest
  -> LoadInstalledProviders
  -> Protocol Registry
  -> Provider Adapter
  -> 外部模型/API
  -> Task / Resource
```

影策 不是通用的后端插件执行平台。上传包不能注册 Gin 路由、GORM 模型、后台 Worker 或任意宿主代码。Web 入口即使在 Manifest 类型中存在，也必须进入 sandbox 或 worker 运行时，不能直接注入主页面。

## 2\. 插件包和注册流程

### 2\.1 包格式

影策 使用 yingce\.plugin/package\-v1 作为 ZIP 容器格式。根目录必须有 manifest\.json。协议包可以附带 README、文档、静态资源和受限的 Web 入口。

标准包结构：

```text
my-provider.yingce-plugin
├── manifest.json
├── README.md
├── docs/
│   └── interface.md
├── assets/
└── web/
    └── entry.js
```

影策 当前官方生成包实际统一为：

```text
README.md
docs/interface.md
manifest.json
```

官方包目录中的 78 个包都没有 Web 入口，没有可执行后端代码，全部是声明式 Provider 包。

### 2\.2 包校验限制

|项目|影策 限制|
|---|---|
|包扩展名|\.yingce\-plugin|
|包格式|yingce\.plugin/package\-v1|
|包大小|不超过 16 MiB|
|文件数量|1 到 256 个|
|单文件解压大小|不超过 8 MiB|
|manifest\.json|不超过 512 KiB|
|必需文件|根目录 manifest\.json|
|允许路径|manifest\.json、web/、assets/、docs/、README\.md、LICENSE|
|路径安全|拒绝绝对路径、路径穿越、反斜杠和重复文件|
|符号链接|不允许|

如果没有 entry，则 Manifest 不能声明 runtime\.web，包中也不能出现 web/ 文件。如果声明 entry，entry 必须位于 web/ 下，且 runtime\.web 必须为 sandbox 或 worker。

解析过程只读取 ZIP、解析 JSON 并执行合同校验，不执行包内 JavaScript。

### 2\.3 官方包的生成

影策 的 plugin\-packages/generate\-catalog\.mjs 集中维护官方协议包规格。它生成每个 Provider 的：

- manifest\.json；

- README\.md；

- docs/interface\.md。

plugin\-packages/build\-packages\.sh 再将这些文件按稳定顺序打包为 \.yingce\-plugin 文件。官方包的文档会根据 Provider 参数、请求模板、响应映射和扩展键自动生成。

这意味着官方协议扩展的推荐方式是：

```text
补充协议规格
  -> 生成 Manifest
  -> 生成接口文档
  -> 打包 .yingce-plugin
  -> 后端启动时加载
```

## 3\. Manifest 版本事实

影策 代码的 ValidateManifest 同时接受：

```text
yingce.plugin/v1
yingce.plugin/v2
```

但在基线提交中，plugin\-packages/generate\-catalog\.mjs 生成的官方协议包使用：

```text
yingce.plugin/v2
```

仓库中的部分前端开发文档和内置插件类型仍使用 yingce\.plugin/v1 示例。这是文档和实现演进过程中的兼容状态，不代表存在两套不同的 Provider 运行时。

对于基于 影策 原始协议包的新 Provider，建议：

- 遵循 generate\-catalog\.mjs 产出的 v2 结构；

- 保留 Provider 的请求/响应声明式映射；

- 不依赖当前 fork 新增的 Application Plugin 字段；

- 用真实的 Manifest 解析、Provider 适配和端到端请求测试确认兼容性。

影策 本身还没有当前仓库引入的结构化应用字段：

```text
skills
capabilities
experiences
workspaces
nativeAdapters
integrity
```

这些字段属于当前仓库后续扩展的应用插件平台，不应写进 影策 版协议插件接入说明。

## 4\. Provider Registry 架构

### 4\.1 Registry

backend/internal/protocol/registry\.go 中的 Registry 保存 Adapter 实例，并提供：

- Register：注册一个 Provider；

- Unregister：从运行时移除 Provider；

- Get：按精确 ID 获取；

- Resolve：按 ID 或 Legacy Alias 获取；

- List：按 surface 和 capability 列出可用 Provider；

- IsCapability：判断 Provider 是否支持某种能力。

一个插件包是安装、启停和权限管理单位；一个包可以包含多个 Provider，Provider 则是 Registry 中独立的执行入口。

影策 当前有一个特殊包 autodl\-comfyui，它包含两个 Provider：

```text
autodl-comfyui
autodl-comfyui-audio
```

因此包数量与 Provider 数量不完全相同。

### 4\.2 Adapter

backend/internal/protocol/adapter\.go 定义协议 Adapter：

```text
Metadata
BuildCreate
ParseCreate
BuildPoll
ParsePoll
BuildCancel
```

可选能力包括：

```text
AgentAdapter
  BuildAgent
  ParseAgent

ResultAdapter
  BuildResult
```

Adapter 只拥有协议翻译职责：

- 将统一请求转换为上游请求；

- 将上游创建响应转换为统一 CreateResult；

- 将上游轮询响应转换为统一 PollResult；

- 将上游 Agent 响应转换为文本、推理和工具调用；

- 将上游结果响应转换为资源可读取的结果。

Adapter 不拥有：

- API Key 或 Secret；

- 任务数据库；

- 轮询租约；

- 计费和退款；

- 资源归属；

- 媒体下载；

- 任务恢复；

- 用户权限；

- 任意宿主路由。

## 5\. 影策 官方插件目录

### 5\.1 目录规模

基于 影策 基线提交中的 plugin\-packages 统计：

|指标|数量|
|---|---|
|官方 \.yingce\-plugin 包|78|
|Provider contribution|79|
|Workflow contribution|11|
|text Provider|29|
|image Provider|20|
|video Provider|29|
|audio Provider|1|
|支持 Agent 请求|21|
|支持异步轮询|38|
|支持取消|4|
|支持单独结果获取|2|
|要求公网媒体 URL|13|
|带 Web entry 的官方包|0|

79 个 Provider 的 capability 总数为 79，因为 autodl\-comfyui 同时贡献视频和音频 Provider。

### 5\.2 文本协议包

```text
anthropic-messages
aws-bedrock-converse
aws-bedrock-invoke-model
azure-openai
baichuan-chat
cohere-chat-v2
dashscope-qwen-native
deepseek-chat
fireworks-chat
google-gemini-generate-content
groq-chat
kimi-chat
litellm-proxy-chat
localai-chat
minimax-text-native
mistral-chat
newapi-chat
nvidia-nim-chat
ollama-chat
openai-chat-completions
openai-responses
openrouter-chat
siliconflow-chat
together-chat
vertex-gemini
vllm-chat
xai-grok-chat
yi-chat
zhipu-glm-chat
```

这些包覆盖 OpenAI Chat Completions、OpenAI Responses、Anthropic Messages、Gemini generateContent、AWS Bedrock、Azure OpenAI、各类 OpenAI\-compatible 网关和本地模型服务。

21 个 Provider 声明了 Agent 请求映射，可以把工作台的工具调用请求转换为上游 tool/function call 结构，并解析工具名称、参数、调用 ID 和部分模型的 thought signature。

### 5\.3 图片协议包

```text
adobe-firefly
baidu-qianfan-image
bfl-flux
dashscope-wanx-image
fal-queue-image
google-gemini-image
ideogram-image
kling-image
minimax-image
openai-images
recraft-image
replicate-prediction-image
runway-image
stability-image
tencent-hunyuan-image
vertex-imagen
volcengine-ark-seedream
volcengine-jimeng-image
xai-grok-images
zhipu-cogview
```

图片 Provider 可以表达：

- 文生图；

- 图生图或图片编辑；

- 多张参考图；

- 输出数量；

- 画幅或尺寸；

- 质量和分辨率；

- 同步结果；

- 异步任务；

- 图片结果下载；

- 厂商专属参数。

### 5\.4 视频协议包

```text
agnes-video-25
agnes-video-v20
autodl-comfyui
baidu-video
comfyui-workflow
dashscope-wan-video
fal-queue-video
google-gemini-veo
kling-video
luma-dream-machine
minimax-hailuo-video-v2
newapi-media-task-v1
newapi-video-generations-v1
novita-video
openai-videos
pika-via-fal
pixverse-video
replicate-prediction-video
rolldek-wan-video
runninghub-workflow
runway-video
seedance-videos-compatible
tencent-hunyuan-video
vertex-veo
vidu-video
volcengine-ark-seedance
volcengine-jimeng-video
xai-video
zhipu-cogvideox
```

视频 Provider 可以表达：

- 文生视频；

- 图生视频；

- 首帧、尾帧和多图参考；

- 参考视频；

- 参考音频或音色；

- 时长；

- 画幅；

- 分辨率；

- 同步生成音频；

- 水印；

- 异步创建和轮询；

- 任务取消；

- 短期媒体 URL 下载。

### 5\.5 音频协议包

影策 的官方包目录只有一个 audio capability：

```text
autodl-comfyui-audio
```

它通过 AutoDL ComfyUI 工作流提供 IndexTTS2 语音能力。该 Provider 与 autodl\-comfyui 视频 Provider 位于同一个插件包中。

## 6\. 统一请求模型

### 6\.1 GenerationRequest

backend/internal/protocol/types\.go 中的 GenerationRequest 是 canvas、creation、agent 共用的协议输入。核心字段包括：

```text
Capability
Model
Prompt
Instructions
Messages
Inputs
Images
Videos
Audios
Duration
AspectRatio
Resolution
Quality
GenerateAudio
Watermark
ImageCount
Operation
Output
ProviderOptions
Extra
```

各字段语义如下：

|字段|作用|
|---|---|
|Capability|text、image、video 或 audio|
|Model|上游模型 ID|
|Prompt|文本或媒体生成提示词|
|Instructions|系统指令或额外指令|
|Messages|对话消息数组|
|Inputs|通用媒体输入|
|Images|图片和图片参考|
|Videos|视频和视频参考|
|Audios|音频、音色或音频参考|
|Duration|视频或音频时长|
|AspectRatio|画幅或尺寸|
|Resolution|分辨率档位|
|Quality|质量档位|
|GenerateAudio|是否同时生成音频|
|Watermark|是否添加水印|
|ImageCount|图片输出数量|
|Operation|text\_to\_video、reference\_to\_video 等业务操作|
|Output|统一输出选项|
|ProviderOptions|Provider 命名空间下的厂商参数|
|Extra|兼容性扩展字段|

Provider\-specific 字段只能放在 Provider 自己的 ProviderOptions 命名空间或 Extra 中。宿主不会解释厂商专属字段，但会把它们交给 Manifest 表达式读取。

### 6\.2 MediaReference

媒体引用包含：

```text
ID
URL
DataURL
Kind
Role
MIMEType
Name
Order
Weight
Metadata
Ephemeral
```

Role 用于表达媒体业务语义，例如：

```text
first_frame
last_frame
reference_image
subject_reference
style_reference
edit_source
reference_video
reference_audio
```

Order 用于保持参考媒体顺序。声明式 Provider 可以按 role 筛选、按 order 排序，再映射为上游所需的字段。

### 6\.3 OutputOptions

OutputOptions 统一描述输出要求：

```text
Count
Duration
AspectRatio
Width
Height
Resolution
Quality
FPS
GenerateAudio
Watermark
Format
Options
```

新 Provider 应优先读取 request\.output，同时兼容宿主传入的历史顶层字段。

## 7\. Manifest Provider 定义

### 7\.1 基本结构

一个 Provider 的核心结构如下：

```json
{
  "id": "acme-video",
  "label": "Acme Video",
  "capabilities": ["video"],
  "scopes": [
    "admin.system-channel",
    "user.custom-channel",
    "canvas",
    "creation",
    "agent"
  ],
  "baseUrl": "https://api.example.com",
  "requiresPublicMediaUrls": false,
  "auth": {
    "type": "bearer",
    "field": "apiKey"
  },
  "parameters": [],
  "create": {},
  "poll": {},
  "cancel": {},
  "result": {},
  "response": {}
}
```

Provider 的 capabilities 只能使用：

```text
text
image
video
audio
```

scopes 只能使用：

```text
admin.system-channel
user.custom-channel
canvas
creation
agent
```

### 7\.2 ManifestOperation

每个操作可以声明：

|字段|用途|
|---|---|
|method|GET、POST、PUT 或 DELETE|
|path|相对路径|
|pathTemplate|动态路径表达式|
|originPath|是否使用同源根路径|
|contentType|请求内容类型|
|contentTypeTemplate|动态内容类型|
|headers|请求头表达式|
|query|查询参数表达式|
|body|JSON 或结构化请求体表达式|
|fields|旧版简单字段映射|
|files|multipart 文件声明|

支持的请求内容类型包括：

```text
application/json
multipart/form-data
application/x-www-form-urlencoded
application/octet-stream
```

请求路径必须是相对路径。模板中的 model 和 taskId 会由宿主进行 URL 路径转义。插件不能通过 pathTemplate 访问任意绝对 URL。

### 7\.3 参数声明

parameters 用于描述工作台传给 Provider 的参数，不等于宿主会自动生成表单。参数可以声明：

```json
{
  "name": "duration",
  "type": "integer",
  "required": false,
  "description": "视频时长，单位为秒",
  "values": ["5", "10"],
  "mapping": "duration/seconds"
}
```

典型统一参数包括：

```text
model
prompt
messages
instructions
images
videos
audios
duration
aspectRatio
resolution
quality
generateAudio
watermark
providerOptions
```

### 7\.4 鉴权声明

影策 官方包实际使用的鉴权类型包括：

|auth\.type|Provider 数量|
|---|---|
|bearer|63|
|anthropic|1|
|aws\-sigv4|2|
|google\-api\-key|3|
|header|4|
|none|2|
|tc3|2|
|volcengine\-v4|2|

鉴权字段只描述宿主如何读取渠道配置，不把密钥放进 Manifest。常见配置字段是 apiKey，AWS 和腾讯云签名协议还需要 secretKey、region、service 等宿主配置。

### 7\.5 Response

响应定义可以使用路径数组或表达式：

```json
{
  "taskIdPaths": ["id", "data.task_id"],
  "statusPaths": ["status", "data.status"],
  "errorPaths": ["error.code", "code"],
  "messagePaths": ["error.message", "message"],
  "textPaths": ["output_text", "choices.0.message.content"],
  "reasoningPaths": ["reasoning_content"],
  "resultPaths": ["data.video_url", "output.url"],
  "resultKind": "video",
  "resultEphemeral": true
}
```

结果解析可以得到：

- taskId；

- status；

- message；

- text；

- reasoning；

- images；

- videos；

- audios；

- usage。

Provider 可以使用直接路径，也可以使用 response 下的表达式字段：

```json
{
  "status": {
    "$coalesce": [
      { "$ref": "response.status" },
      { "$ref": "response.state" },
      "pending"
    ]
  }
}
```

## 8\. 声明式表达式引擎

### 8\.1 设计目标

影策 使用 JSON 表达式替代上传代码。表达式由后端解释器执行，只能访问受控的 request、response、taskId 和局部迭代变量，不能访问宿主对象、文件系统、数据库或网络客户端。

### 8\.2 表达式列表

|类别|操作|
|---|---|
|引用|$ref、$literal|
|默认值|$coalesce、$default、$omitEmpty|
|字符串|$concat、$lower、$upper、$trim、$toString、$toInt、$toBool|
|数组|$map、$filter、$concatArrays、$first、$last、$at、$len、$sortByOrder|
|对象|$indexObject、$merge|
|条件|$if、$switch|
|媒体和 JSON|$dataMime、$dataPayload、$json|
|数值|$add、$multiply、$min、$max、$ceilStep|
|比较逻辑|$eq、$ne、$gt、$gte、$lt、$lte、$in、$and、$or、$not|

### 8\.3 常见使用方式

按角色选取首尾帧：

```json
{
  "first_frame": {
    "$omitEmpty": {
      "$first": {
        "$map": {
          "from": {
            "$filter": {
              "from": { "$sortByOrder": { "$ref": "request.images" } },
              "as": "media",
              "where": { "$eq": [{ "$ref": "media.role" }, "first_frame"] }
            }
          },
          "as": "media",
          "in": { "$ref": "media.value" }
        }
      }
    }
  }
}
```

按顺序生成多个参考图字段：

```json
{
  "$indexObject": {
    "from": { "$sortByOrder": { "$ref": "request.images" } },
    "as": "media",
    "prefix": "ref_image_",
    "max": 9,
    "value": { "$ref": "media.value" }
  }
}
```

按模型选择不同参数：

```json
{
  "$if": {
    "condition": {
      "$in": [
        { "$ref": "request.model" },
        ["model-a", "model-b"]
      ]
    },
    "then": { "mode": "reference" },
    "else": { "mode": "text" }
  }
}
```

合并 ProviderOptions 中的完整厂商对象：

```json
{
  "$merge": [
    {
      "model": { "$ref": "request.model" },
      "prompt": { "$ref": "request.prompt" }
    },
    {
      "$coalesce": [
        { "$ref": "request.providerOptions.acme-video.body" },
        {}
      ]
    }
  ]
}
```

### 8\.4 表达式边界

表达式适合：

- 字段路径读取；

- 空值省略；

- 数组映射和过滤；

- 媒体 role 与 order 处理；

- 条件字段；

- 参数合并；

- 字符串、数字和 MIME 转换；

- JSON、查询参数、请求头和 multipart 文件构造。

表达式不适合：

- 任意循环或递归；

- 读取文件系统；

- 访问数据库；

- 发起额外 HTTP 请求；

- 签名算法；

- 复杂业务状态机；

- 自定义 Worker；

- 绕过宿主安全、计费或权限。

如果协议需要这些能力，应增加宿主 Adapter，而不是把脚本塞进 Manifest。

## 9\. 影策 的实际扩展类型

Manifest 类型中声明了多个 contribution：

|contribution|协议层是否有字段|影策 实际状态|
|---|---|---|
|providers|是|官方目录主要能力，已通用执行|
|workflows|是|Manifest 可描述，AutoDL 包有 11 个入口；具体执行仍由 Provider/宿主处理|
|canvasNodes|是|前端类型和内置注册机制存在，上传包不能自动注入主页面节点代码|
|transforms|是|类型合同存在，需宿主运行时支持|
|commands|是|可声明命令元数据，不能携带任意执行器|
|assetSources|是|Eagle 通过受信任内置实现接入|
|usageObservers|是|协议字段存在，需宿主对应服务|
|aiCapabilities|是|提示词优化器通过受信任内置实现接入|
|agents|是|Agent/MCP 相关声明存在，需宿主 Agent 边界|
|importExport|是|可声明导入导出贡献，需宿主对应 UI/服务|

因此，影策 的“Manifest 类型支持”要分为两层理解：

1. Provider 协议扩展已经是稳定的通用上传路径。

2. 其他 contribution 主要是统一插件模型和前端内置插件的扩展接口，不能只上传一个 JSON 就自动获得完整执行能力。

## 10\. 前端内置插件和宿主 API

### 10\.1 前端插件类型

web/src/lib/plugins/plugin\-types\.ts 定义了前端插件类型，包括：

- PluginManifest；

- PluginHostContext；

- PluginStorage；

- PluginTextRequest；

- PluginTextResponse；

- AssetSourceProvider；

- PromptOptimizerProvider；

- RegisteredPlugin；

- PluginInstallation。

PluginRuntime 支持的声明值包括：

```text
declarative
sandbox
worker
trusted-backend
```

PluginSurface 支持：

```text
node
fullscreen
hybrid
asset-source
settings
```

### 10\.2 宿主上下文

宿主上下文包含：

```text
manifest
permissions
storage
config
services
```

其中 services 可提供：

- ai\.text：通过当前已配置文本模型执行带工具调用的请求；

- media\.resolve：解析媒体引用为可使用的媒体内容；

- usage\.list：读取受控用量信息。

插件存储按插件 ID 隔离，不能使用页面的任意 localStorage key。

### 10\.3 内置 Eagle

Eagle 是 影策 已接通的外部素材连接器。它通过影策后端访问 Eagle Local API，前端不直接访问 Eagle 本机文件路径。

已接通能力：

- 读取 Eagle 资料库和文件夹树；

- 按文件夹、关键词、分页浏览素材；

- 读取缩略图和原文件；

- 将图片、视频、音频和模型文件导入工作台；

- 将图片、视频和音频写回 Eagle；

- 创建 Eagle 文件夹；

- 将生成结果自动归档到指定文件夹；

- 保留 Eagle item ID、folder ID、路径、标签、扩展名和来源 metadata。

不支持：

- 将文本或实体转换成工作台文本素材；

- 删除 Eagle 原始素材；

- 批量覆盖和冲突解决；

- 让远程服务器任意访问用户电脑上的 Eagle。

### 10\.4 内置提示词优化器

prompt\-optimizer 使用宿主 ai\.text 服务，不读取 API Key，不自己创建模型客户端。

支持五种模式：

```text
expand
refine
style
model-adapt
reference
```

输出结构包括：

```text
optimizedPrompt
negativePrompt
changes
assumptions
variants
modelProfile
```

结果先以可编辑内容展示，用户确认后才回填当前输入框。它会根据目标生成模式和模型族调整提示词表达，但不会自行添加未经确认的模型参数或控制语法。

### 10\.5 内置工作流插件

影策 的前端和后端都包含两个工作流插件：

- RunningHub Workflow；

- ComfyUI Workflow / Bridge。

它们可以在画布中提供图片、视频和音频工作流入口。API Key、工作流配置、字段映射和 Bridge 设备信息由宿主设置和后端服务维护，插件 Manifest 不保存密钥。

### 10\.6 肖像权可识别性排查

影策 还包含肖像权可识别性排查内置插件，用于：

- 本地人脸预检；

- 网络候选排查；

- 人脸或图像候选去重；

- 生成审慎风险报告；

- 在画布节点或全屏工作台中展示排查结果。

它是仓库内受信任的实现，不代表上传的第三方包可以自动获得本地 ONNX、网络检索或报告生成执行能力。

## 11\. 插件生命周期和 HTTP 接口

### 11\.1 生命周期

```text
发现
  -> 读取 Manifest
  -> 上传/安装
  -> ZIP 与 Manifest 校验
  -> 注册 Provider
  -> 管理员启用或停用
  -> 用户侧选择可用渠道
  -> 执行任务
  -> 更新或卸载
```

影策 运行时会区分：

- 官方 bundled 插件；

- 管理员上传的 uploaded 插件；

- enabled、disabled、invalid 状态；

- platform availability；

- 用户级的应用插件启用状态。

官方插件不能被上传包覆盖。上传包安装失败时不应注册半套 Provider。

### 11\.2 用户插件接口

核心接口位于 /api/plugins：

|方法|路径|用途|
|---|---|---|
|GET|/api/plugins|获取用户可见插件和状态|
|GET|/api/plugins/status|获取插件和工作流运行状态|
|GET|/api/plugins/catalog|按 scope/capability 获取 Provider 目录|
|POST|/api/plugins|管理员上传插件包|
|GET|/api/plugins/:id/package|下载可见插件包|
|PUT|/api/plugins/:id/activation|更新用户级插件状态|
|DELETE|/api/plugins/:id|管理员卸载上传插件|

管理员平台接口位于 /api/admin/plugins：

|方法|路径|用途|
|---|---|---|
|GET|/api/admin/plugins|查看完整插件目录和启用人数|
|PUT|/api/admin/plugins/:id/availability|设置平台可用性|

官方工作流插件另外提供 RunningHub、ComfyUI 和 Eagle 相关的受控业务接口。

### 11\.3 运行状态

协议运行时使用 PluginPlatformState 和 UserPluginState 分离平台状态与用户状态：

```text
effectiveEnabled = platformAvailable && userEnabled
```

系统级协议 Provider 通常由管理员控制。官方工作流、Eagle 和提示词能力可以按用户启用。管理员停用后，新请求不能进入；已经接收的任务应按任务系统的终态策略继续处理或明确失败，不应伪造成功。

## 12\. 接入前的路径选择

### 12\.1 只接入一个模型或 API

选择声明式 protocol Provider。适用情况：

- 上游是 HTTP/HTTPS API；

- 请求可以由统一字段和 ProviderOptions 表达；

- 响应可以由路径或声明式表达式解析；

- 异步流程是创建、轮询、可选取消或结果获取；

- 不需要新增数据库、Worker 或业务状态机。

### 12\.2 接入工作流型 API

仍然先使用 Provider \+ workflows。适用情况：

- 上游是 ComfyUI、RunningHub、AutoDL 或类似工作流服务；

- 模型入口由 workflow ID 或版本决定；

- 请求体包含厂商工作流参数；

- 需要把图片、视频或音频按角色映射到工作流字段。

如果必须访问复杂上游协议、需要动态签名或需要执行专门的任务协调，应由宿主实现受控 Adapter，再由插件声明入口。

### 12\.3 增加完整创作应用

原始 影策 不提供当前仓库中的 Application Plugin、Experience、Workspace 和 SkillRun 体系。若需求是：

- 多步创作流程；

- 用户表单和 Action；

- 可暂停、恢复和审批；

- 项目 Unit；

- 结构化 Artifact；

- 结果 Workbench；

- 个人模板和完整归档；

则不能只按照 影策 Provider 包接入，应使用当前仓库新增的应用插件平台合同。

## 13\. 新增 Protocol Provider 接入指南

### 第一步：确定 Provider 身份

确定：

```text
插件 ID
Provider ID
显示名称
供应商
capability
可用 scope
默认 Base URL
鉴权类型
同步或异步
```

插件 ID 和 Provider ID 使用稳定的小写标识。一个包内的 Provider ID 不能重复。

### 第二步：设计统一字段

优先使用：

```text
request.model
request.prompt
request.instructions
request.messages
request.images
request.videos
request.audios
request.duration
request.aspectRatio
request.resolution
request.quality
request.output
request.providerOptions.<provider-id>
request.extra
```

如果参考媒体有业务角色，应读取 role，而不是在插件里猜测第 0 张或第 1 张图片。只有上游协议明确使用顺序时，才按 order 生成数组或编号字段。

### 第三步：编写 Manifest

下面是一个基于 影策 v2 Provider 合同的异步视频示例：

```json
{
  "apiVersion": "yingce.plugin/v2",
  "id": "acme-video",
  "name": "Acme Video",
  "version": "1.0.0",
  "author": "Acme",
  "description": "Acme 视频生成协议插件。",
  "permissions": ["generation.run", "media.read"],
  "configuration": {
    "fields": [
      {
        "name": "apiKey",
        "type": "secret",
        "label": "API Key",
        "required": true
      }
    ]
  },
  "contributes": {
    "providers": [
      {
        "id": "acme-video",
        "label": "Acme Video",
        "capabilities": ["video"],
        "scopes": [
          "admin.system-channel",
          "user.custom-channel",
          "canvas",
          "creation",
          "agent"
        ],
        "baseUrl": "https://api.example.com",
        "auth": {
          "type": "bearer",
          "field": "apiKey"
        },
        "parameters": [
          {
            "name": "model",
            "type": "string",
            "required": true,
            "mapping": "model",
            "description": "上游模型 ID。"
          },
          {
            "name": "prompt",
            "type": "string",
            "required": true,
            "mapping": "prompt",
            "description": "视频提示词。"
          },
          {
            "name": "duration",
            "type": "integer",
            "mapping": "duration",
            "description": "视频时长，单位为秒。"
          },
          {
            "name": "resolution",
            "type": "string",
            "mapping": "resolution",
            "values": ["720p", "1080p"]
          }
        ],
        "create": {
          "method": "POST",
          "path": "/v1/videos",
          "contentType": "application/json",
          "body": {
            "model": { "$ref": "request.model" },
            "prompt": { "$ref": "request.prompt" },
            "duration": { "$omitEmpty": { "$ref": "request.duration" } },
            "resolution": { "$omitEmpty": { "$ref": "request.resolution" } }
          }
        },
        "poll": {
          "method": "GET",
          "path": "/v1/videos/{{taskId}}"
        },
        "response": {
          "taskIdPaths": ["id", "data.id", "data.task_id"],
          "statusPaths": ["status", "data.status"],
          "messagePaths": ["message", "error.message"],
          "resultPaths": ["video_url", "data.video_url", "output.url"],
          "resultKind": "video",
          "resultEphemeral": true
        }
      }
    ]
  }
}
```

如果使用 影策 v1 兼容格式，运行时仍能解析，但应以目标宿主实际支持的协议版本和现有官方模板为准。

### 第四步：处理厂商扩展字段

厂商专属参数放在 Provider 自己的命名空间：

```text
request.providerOptions.acme-video
```

例如：

```json
{
  "body": {
    "$merge": [
      {
        "model": { "$ref": "request.model" },
        "prompt": { "$ref": "request.prompt" }
      },
      {
        "$coalesce": [
          { "$ref": "request.providerOptions.acme-video.body" },
          {}
        ]
      }
    ]
  }
}
```

不要读取其他 Provider 的扩展命名空间，也不要在一个包中用模型名称猜测不属于本协议的能力。

### 第五步：处理参考素材

如果上游支持参考图：

- 在参数中声明 images；

- 用 media\.role 过滤 first\_frame、last\_frame、reference\_image 等角色；

- 用 media\.order 保持顺序；

- 用 media\.value 取得宿主为 Provider 准备的媒体值；

- 如果上游要求公网地址，声明 requiresPublicMediaUrls: true；

- 不要把短期签名 URL 写入任务或 Manifest；

- 不要把本地文件路径当成上游 URL。

如果上游需要 multipart：

```json
{
  "contentType": "multipart/form-data",
  "files": [
    {
      "name": "image",
      "source": { "$ref": "request.images" },
      "filename": { "$coalesce": [{ "$ref": "request.images.0.name" }, "reference.png"] },
      "mimeType": { "$coalesce": [{ "$ref": "request.images.0.mimeType" }, "image/png"] }
    }
  ]
}
```

文件字段必须使用受支持的 ManifestFilePart 合同，不能用表达式执行自定义文件读取。

### 第六步：处理异步任务

异步 Provider 至少需要：

```text
create
poll
response
```

只有上游确实支持时才增加：

```text
cancel
result
```

状态映射必须覆盖真实上游状态。成功状态但没有可识别媒体结果时，应该返回协议错误，不要生成假 URL。

短期媒体结果必须使用：

```text
resultEphemeral: true
```

宿主会在任务完成后尽快下载并保存为工作台资源。

### 第七步：本地打包

最小包：

```text
acme-video.yingce-plugin
├── manifest.json
├── README.md
└── docs/interface.md
```

协议包不需要 Web 入口。如果带 Web 入口：

```text
entry: web/entry.js
runtime.web: sandbox
```

不要把以下内容放进包中：

```text
API Key
Secret
Cookie
Authorization header
签名 URL
对象存储内部 key
本地绝对路径
任意 Go/Node Worker
Gin 路由
数据库迁移
```

## 14\. 校验和测试指南

### 14\.1 Manifest 静态校验

至少检查：

- apiVersion 为支持版本；

- id、Provider ID 和版本合法；

- capabilities 只使用四种统一能力；

- scopes 只使用五种宿主范围；

- Provider ID 不重复；

- create 的 HTTP 方法受支持；

- path 是相对路径；

- contentType 受支持；

- files 只在 multipart 合同下出现；

- agent 有对应 agentResponse；

- poll、cancel、result 的路径和参数完整；

- validations 有断言和错误消息；

- 没有敏感字符串。

### 14\.2 协议行为测试

至少准备：

```text
文生文本成功
工具调用文本成功
文生图成功
参考图生图成功
文生视频成功
首尾帧视频成功
异步任务处理中
异步任务成功
异步任务失败
任务取消
结果 URL 过期或下载失败
上游 HTTP 非 2xx
上游返回非 JSON
缺少 task ID
成功状态缺少媒体结果
```

### 14\.3 资源和安全测试

确认：

- API Key 只从渠道配置读取；

- API Key 不出现在 Manifest、任务正文、日志或 URL；

- 私网和本机上游遵守宿主的 SSRF 策略；

- requiresPublicMediaUrls 的 Provider 不会收到不可访问的本机地址；

- 结果资源归属于当前用户；

- 任务失败不会被包装成成功；

- 插件停用后不再接收新任务；

- 更新失败时旧版本仍然可用；

- 上传包不会执行任意代码。

## 15\. 不能只靠 Manifest 解决的需求

以下需求超出 影策 声明式 Provider 的边界：

- 新增后台数据库表；

- 新增专用后台 Worker；

- 新增复杂任务状态机；

- 新增自定义计费规则；

- 新增跨用户资源访问；

- 新增任意外部系统写入；

- 新增动态签名算法；

- 新增复杂媒体预处理；

- 新增结构化可编辑 Workbench；

- 新增完全独立的全屏应用；

- 让插件直接访问用户 Cookie 或 API Key；

- 让远程 JS 直接操作主页面 React 状态。

这类需求应先扩展宿主服务、Adapter、Registry、权限和测试，再让插件使用受控入口。

## 16\. 影策 与当前仓库的边界

当前仓库在 影策 之上增加了完整的 Application Plugin 平台，主要包括：

```text
CanvasPluginVersion
Primary Skill / Internal Skill
Action
Capability
Experience
Workspace
SkillRun
Workflow Runner
ToolAdapter Registry
Artifact
Workbench Registry
AgentShell
个人模板
Experience Archive
```

这些不是 影策 基线中的原生协议插件能力。它们通过扩展后的 Manifest 和独立的应用包校验、版本、项目、工作流和资源模型实现。

因此，接入时应按下面的判断：

|需求|采用|
|---|---|
|接入一个模型/API|影策 Protocol Provider|
|接入异步图片/视频任务|Provider \+ poll/result/cancel|
|接入 ComfyUI/RunningHub 工作流|Provider \+ workflows|
|让 Agent 调用文本模型|Provider 的 agent/agentResponse|
|让用户完成一套多步影视创作|当前仓库 Application Plugin|
|需要项目 Unit 和结构化结果|当前仓库 Workspace/Artifact/Workbench|
|需要审批、恢复、批量步骤|当前仓库 Workflow Runner|
|需要新的后台工具执行器|宿主 ToolAdapter|
|需要任意服务端代码|独立服务或宿主改造|

## 17\. 代码索引

### 影策 插件协议

- backend/internal/protocol/types\.go：统一请求、媒体引用、Manifest 和 Provider 类型。

- backend/internal/protocol/adapter\.go：Adapter、AgentAdapter 和 ResultAdapter。

- backend/internal/protocol/manifest\.go：Manifest 校验、Provider 加载、请求构造和响应解析。

- backend/internal/protocol/expression\.go：声明式 JSON 表达式解释器。

- backend/internal/protocol/package\.go：ZIP 包和 entry 校验。

- backend/internal/protocol/registry\.go：Provider Registry。

- backend/internal/protocol/documentation\.go：官方协议文档生成。

### 影策 插件运行时

- backend/internal/service/protocol\_plugins\.go：官方包引导、安装、启停、卸载和运行时重载。

- backend/internal/service/protocol\_registry\.go：Provider 目录、scope 和 capability 投影。

- backend/internal/service/plugin\_management\.go：平台状态、用户状态和权限管理。

- backend/internal/handler/plugin\.go：用户和管理员插件 HTTP 接口。

- backend/internal/service/workflow\_plugins\.go：RunningHub 和 ComfyUI 工作流插件。

- plugin\-packages/generate\-catalog\.mjs：官方协议包生成器。

- plugin\-packages/build\-packages\.sh：官方包打包脚本。

### 影策 前端插件

- web/src/lib/plugins/plugin\-types\.ts：前端 Manifest、宿主上下文、素材源和 AI 能力类型。

- web/src/lib/plugins/plugin\-registry\.ts：前端插件注册中心。

- web/src/lib/plugins/plugin\-storage\.ts：插件命名空间存储。

- web/src/services/plugin\-host\.ts：宿主 AI 文本服务上下文。

- web/src/lib/plugins/builtin/eagle\.ts：Eagle 素材连接器。

- web/src/lib/plugins/builtin/prompt\-optimizer\.ts：提示词优化器。

- web/src/lib/plugins/builtin/workflows\.ts：RunningHub 和 ComfyUI 工作流插件。

- web/src/lib/plugins/builtin/portrait\-clearance\.ts：肖像权可识别性排查。

### 官方说明和示例

- docs/content/docs/plugins/plugin\-system\.mdx：影策 插件系统设计和安全边界。

- docs/content/docs/plugins/plugin\-surfaces\.mdx：node、fullscreen 和 hybrid 显示形态。

- web/src/pages/plugins/plugin\-development\-guide\.md：插件包、Manifest 和 Provider 接入说明。

- backend/internal/protocol/docs/：内置协议文档。

- plugin\-packages/：官方 \.yingce\-plugin 包。

## 18\. 总结

影策 插件的核心是“声明式 Provider 协议包”：它把不同模型服务转换成统一的文本、图片、视频和音频请求，并覆盖 Agent、异步任务、参考媒体、云签名和结果下载等协议差异。

它已经能支撑大量模型/API 接入，但扩展的基本单位仍是协议适配器。它不能单独承载完整的影视创作业务流程、项目空间、审批状态机或结构化工作台。

当前仓库的 Application Plugin 平台是在这套底座之上新增的更高层运行模型。两者的边界应保持清晰：

```text
Protocol Plugin
  负责“如何调用一个外部模型或服务”

Application Plugin
  负责“用户如何完成一套可恢复的创作流程”
```
